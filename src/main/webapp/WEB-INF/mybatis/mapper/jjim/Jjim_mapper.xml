<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Jjim_mapper">
   <select id="getJjimListByUserId" parameterType="String" resultType="JjimVO">
        SELECT j.post_id as postId, j.user_id as userId, p.title
        FROM Jjim j
        JOIN Posts p ON j.post_id = p.post_id
        WHERE j.user_id = #{userId}
    </select>

    <select id="checkJjim" parameterType="Map" resultType="int">
        SELECT COUNT(*)
        FROM Jjim
        WHERE post_id = #{postId} AND user_id = #{userId}
    </select>
    

    <insert id="addJjim" parameterType="JjimVO">
        INSERT INTO Jjim (post_id, user_id)
        VALUES (#{postId}, #{userId})
    </insert>

    <delete id="removeJjim" parameterType="Map">
        DELETE FROM Jjim
        WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>
	
</mapper>