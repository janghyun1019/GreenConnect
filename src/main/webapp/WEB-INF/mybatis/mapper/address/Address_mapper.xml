<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Address_mapper">
    <select id="findAllByUserId" resultType="Address">
        SELECT id, user_id, postal_code, address1, address2, receiver, phone, is_default
        FROM address
        WHERE user_id = #{userId}
    </select>

    <select id="findId" resultType="Address">
        SELECT id, user_id, postal_code, address1, address2, receiver, phone, is_default
        FROM address
        WHERE id = #{id}
    </select>

    <insert id="saveAddress" parameterType="Address">
        INSERT INTO address (id, user_id, postal_code, address1, address2, receiver, phone, is_default)
        VALUES (address_seq.NEXTVAL, #{userId}, #{postalCode}, #{address1}, #{address2}, #{receiver}, #{phone}, #{isDefault})
    </insert>

    <update id="updateAddress" parameterType="Address">
        UPDATE address
        SET postal_code = #{postalCode},
            address1 = #{address1},
            address2 = #{address2},
            receiver = #{receiver},
            phone = #{phone},
            is_default = #{isDefault}
        WHERE id = #{addrseeId} <!-- DTO 필드명과 일치 -->
    </update>

    <delete id="deleteAddress" parameterType="int">
        DELETE FROM address
        WHERE id = #{id}
    </delete>

    <update id="updateAllSetDefaultFalse">
        UPDATE address <!-- 테이블명 오타 수정 -->
        SET is_default = 'N' <!-- Oracle CHAR(1) 사용 시 'N' -->
        WHERE user_id = #{userId} AND id != #{excludeAddressId}
    </update>
</mapper>