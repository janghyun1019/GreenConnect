<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Admin_mapper">
	<select id="getAdminDashboardData" resultType="Admin">
        SELECT user_count, daily_sales, weekly_sales, monthly_sales, 
               new_users, pending_reports, transaction_count, system_status
        FROM AdminDashboard
        WHERE stat_date = (SELECT MAX(stat_date) FROM AdminDashboard)
	</select>

</mapper>