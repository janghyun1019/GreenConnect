<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Trade_mapper">
    <select id="getOrderList" resultType="com.app.dto.trade.Trade">
        SELECT trade_id AS tradeId, buy_id AS buyId, user_id AS userId, 
               total_price AS totalPrice, payment_method AS paymentMethod, 
               payment_status AS paymentStatus, delivery_status AS deliveryStatus, 
               TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS') AS createdAt 
        FROM TRADE
    </select>

    <update id="processPayment">
        UPDATE TRADE 
        SET payment_status = '결제 완료' 
        WHERE trade_id = #{tradeId}
    </update>

    <select id="getShippingStatus" resultType="String">
        SELECT delivery_status 
        FROM TRADE 
        WHERE trade_id = #{tradeId}
    </select>

    <update id="updateShippingStatus">
        UPDATE TRADE 
        SET delivery_status = #{deliveryStatus} 
        WHERE trade_id = #{tradeId}
    </update>

    <insert id="handleDispute">
        INSERT INTO dispute_history (trade_id, user_id, reason) 
        VALUES (#{tradeId}, #{userId}, #{disputeReason})
    </insert>

    <update id="processRefund">
        UPDATE TRADE 
        SET payment_status = '환불 완료' 
        WHERE trade_id = #{tradeId}
    </update>

    <select id="getRefundHistory" resultType="com.app.dto.trade.Trade">
        SELECT trade_id AS tradeId, buy_id AS buyId, user_id AS userId, 
               total_price AS totalPrice, payment_method AS paymentMethod, 
               payment_status AS paymentStatus, delivery_status AS deliveryStatus, 
               TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS') AS createdAt 
        FROM TRADE 
        WHERE payment_status = '환불 완료'
    </select>
</mapper>